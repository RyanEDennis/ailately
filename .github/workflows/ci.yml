name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: House style (zero via negativa, zero anaphora, word counts)
        run: npm run lint:style
      - name: Guardrails (no LinkedIn, no subscriber tables, PII allowlist, sourced moves)
        run: npm run test:guardrails
      - name: Production build
        run: npm run build
